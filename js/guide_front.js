$(document).ready(function(event) {

    // 추천상품 slider
    var recomm_slider = new Swiper('.guide_slider', {
        loop: false,
        pagination: {
            el: '.swiper-pagination'
        },
        slidesPerView: 1
    });
});